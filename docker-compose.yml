version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}